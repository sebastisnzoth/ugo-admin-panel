import test from'node:test'
import assert from'node:assert/strict'
import{readFile}from'node:fs/promises'
const source=path=>readFile(new URL(`../../${path}`,import.meta.url),'utf8')
test('provider Hugo receives own work history and today earnings from UGO data',async()=>{const api=await source('api/test.ts');assert.match(api,/DATOS UGO DEL PROVEEDOR AUTENTICADO/);assert.match(api,/TRABAJOS COMPLETADOS HOY/);assert.match(api,/GANANCIA NETA HOY/);assert.match(api,/ÚLTIMOS TRABAJOS COMPLETADOS/);assert.match(api,/nunca digas que no tenés acceso al historial/i);assert.match(api,/America\/Sao_Paulo/);assert.match(api,/\.eq\('proveedor_id',userId\)/)})
test('provider Hugo only navigates to earnings or history on explicit navigation intent',async()=>{const[bridge,commands]=await Promise.all([source('src/mvp/provider/ProviderHugoBridge.tsx'),source('src/features/provider/voice/providerVoiceCommands.ts')]);assert.match(commands,/ganancias\?\|ganhos\?\|cobros\?\|saldo\|dinheiro/);assert.match(commands,/historial\|atividade\|actividad/);assert.match(commands,/abrir\|abre\|ver\|mostrar\|mostra\|ir/);assert.match(bridge,/companion_mode:true/)})
test('provider Hugo lifecycle commands use the guarded provider flow and never bypass arrival GPS',async()=>{const commands=await source('src/features/provider/voice/providerVoiceCommands.ts');assert.match(commands,/data\.advance\('en_camino'\)/);assert.match(commands,/data\.advance\('llegado'\)/);assert.match(commands,/data\.service\.estado!=='en_camino'/);assert.match(commands,/GPS preciso/);assert.doesNotMatch(commands,/from\('servicios'\)\.update\(\{estado:/);assert.match(commands,/evidencia inicial es obligatoria/);assert.match(commands,/evidencia final/)})
