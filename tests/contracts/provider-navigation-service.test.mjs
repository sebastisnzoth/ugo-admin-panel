import test from'node:test'
import assert from'node:assert/strict'
import{readFile}from'node:fs/promises'
const read=p=>readFile(new URL('../../'+p,import.meta.url),'utf8')
test('provider root delegates routing decisions to feature navigation',async()=>{const[root,nav]=await Promise.all([read('src/mvp/provider/ProviderRoot.tsx'),read('src/features/provider/navigation/providerNavigation.ts')]);assert.match(root,/providerAcceptedDestination/);assert.match(root,/providerNoticeDestination/);assert.doesNotMatch(root,/ACTIONABLE_SCHEDULE_LEAD_MS/);assert.match(nav,/60\*60\*1000/);for(const type of ['nueva_oferta','trabajo_asignado','pago_liberado','pago_efectivo_confirmado','servicio_completado','servicio_cancelado'])assert.match(nav,new RegExp(type));assert.match(nav,/notice\.tipo\.includes\('disputa'\)/)})

test('provider closure notices preserve the exact service through history navigation',async()=>{const[flow,root,history,nav]=await Promise.all([read('src/mvp/provider/providerFlow.tsx'),read('src/mvp/provider/ProviderRoot.tsx'),read('src/mvp/ServiceHistoryPanel.tsx'),read('src/features/provider/navigation/providerNavigation.ts')]);assert.match(nav,/\['pago_efectivo_confirmado','servicio_completado','servicio_cancelado'\][\s\S]*screen:'history'[\s\S]*id:serviceId/);assert.match(flow,/historyServiceId/);assert.match(flow,/setHistoryServiceId\(next==='history'\?id:null\)/);assert.match(root,/initialServiceId=\{flow\.historyServiceId\}/);assert.match(history,/initialServiceId\?:string\|null/);assert.match(history,/\.eq\('id',initialServiceId\)\.eq\('proveedor_id',userId\)\.maybeSingle\(\)/);assert.match(history,/setProviderDetail\(data as Row\)/)})
