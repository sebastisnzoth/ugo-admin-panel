import test from'node:test'
import assert from'node:assert/strict'
import{readFile}from'node:fs/promises'
const read=path=>readFile(new URL('../../'+path,import.meta.url),'utf8')
test('future scheduled acceptance opens agenda instead of an inactive job screen',async()=>{const[root,nav]=await Promise.all([read('src/mvp/provider/ProviderRoot.tsx'),read('src/features/provider/navigation/providerNavigation.ts')]);assert.match(nav,/ACTIONABLE_SCHEDULE_LEAD_MS=60\*60\*1000/);assert.match(nav,/isFuture=scheduledAt!=null[\s\S]*scheduledAt>now\+ACTIONABLE_SCHEDULE_LEAD_MS/);assert.match(root,/alreadyWorking=Boolean\(data\.service&&\['asignado','en_camino','llegado','en_progreso'\]\.includes\(data\.service\.estado\)\)/);assert.match(nav,/alreadyWorking\|\|isFuture\?\{screen:'agenda'/);assert.match(root,/providerAcceptedDestination\(opportunity,alreadyWorking\)/)})
test('passive waiting or dispute never hides a newly actionable assignment',async()=>{const service=await read('src/mvp/provider/providerService.ts');assert.match(service,/MISSION_SERVICE_STATES=new Set\(\['en_camino','llegado','en_progreso'\]\)/);assert.match(service,/PASSIVE_SERVICE_STATES=new Set\(\['esperando_aprobacion','disputado'\]\)/);const mission=service.indexOf('rows.find(service=>MISSION_SERVICE_STATES.has(service.estado))'),immediate=service.indexOf("rows.find(service=>service.estado==='asignado'&&!scheduleTime(service))"),scheduled=service.indexOf("rows.filter(service=>service.estado==='asignado')"),passive=service.indexOf('rows.find(service=>PASSIVE_SERVICE_STATES.has(service.estado))');assert.ok(mission>=0&&immediate>mission&&scheduled>immediate&&passive>scheduled)})
