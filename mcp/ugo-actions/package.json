{
  "name": "ugo-actions-mcp",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Local MCP action layer for UGO. Deterministic, service-scoped tools for Codex/Hugo development.",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/currentJob.js && node --check src/serviceReads.js && node --check src/contextReads.js && node --check src/arrival.js && node --check src/acceptJob.js",
    "test": "node --test test/*.test.mjs",
    "validate": "npm run check && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.1.0",
    "zod": "^4.0.0"
  }
}
