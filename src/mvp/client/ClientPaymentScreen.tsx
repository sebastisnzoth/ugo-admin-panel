import React,{useEffect,useState}from'react'
import{useRoleSession}from'../shared'
import{ClientSummaryScreen}from'./ClientSummaryScreen'
import'./client-payment-screen.css'
type Method='pix'|'cash'
export function ClientPaymentScreen({onBack,onConfirm}:{onBack:()=>void;onConfirm:()=>void}){const{session}=useRoleSession('client'),[method,setMethod]=useState<Method>('pix'),[summary,setSummary]=useState(false),key=session?`ugo:guided-request-draft:${session.user.id}`:'';useEffect(()=>{if(!key)return;try{const d=JSON.parse(sessionStorage.getItem(key)||'{}');if(d.paymentMethod==='cash'||d.paymentMethod==='pix')setMethod(d.paymentMethod)}catch{}},[key]);const next=()=>{try{const d=JSON.parse(sessionStorage.getItem(key)||'{}');sessionStorage.setItem(key,JSON.stringify({...d,paymentMethod:method}))}catch{}setSummary(true)};if(!session)return null;if(summary)return <ClientSummaryScreen onBack={()=>setSummary(false)} onConfirm={onConfirm}/>;return <main className="ugo-payment-screen"><header><button onClick={onBack}>←</button><strong>UGO</strong><div><i/><i/><i/><i/><i/></div><small>4 de 5</small></header><section><h1>¿Cómo vas a pagar?</h1><p>Seguro y simple, como Uber</p><button className={method==='pix'?'selected':''} onClick={()=>setMethod('pix')}><span>◆</span><div><b>PIX</b><small>Pago digital</small></div><i/></button><button className={method==='cash'?'selected':''} onClick={()=>setMethod('cash')}><span>💵</span><div><b>Efectivo</b><small>Pagás al profesional</small></div><i/></button><aside>▣ <span><b>El pago se confirma</b><small>solo cuando se complete el servicio</small></span></aside></section><footer><button onClick={next}>Continuar →</button></footer></main>}
export default ClientPaymentScreen
