import React,{useEffect,useState}from'react'
import{useRoleSession}from'../shared'
import{ClientPaymentScreen}from'./ClientPaymentScreen'
import'./client-when-screen.css'
type When='ahora'|'hoy'|'manana'|'programar';type Draft={when?:When;whenConfirmed?:boolean;scheduleAt?:string;urgent?:boolean}
const localValue=(date:Date)=>{const pad=(n:number)=>String(n).padStart(2,'0');return `${date.getFullYear()}-${pad(date.getMonth()+1)}-${pad(date.getDate())}T${pad(date.getHours())}:${pad(date.getMinutes())}`}
export function ClientWhenScreen({onBack,onContinue}:{onBack:()=>void;onContinue:()=>void}){const{session}=useRoleSession('client'),[when,setWhen]=useState<When>('ahora'),[scheduleAt,setScheduleAt]=useState(''),[message,setMessage]=useState(''),[payment,setPayment]=useState(false),key=session?`ugo:guided-request-draft:${session.user.id}`:'';useEffect(()=>{if(!key)return;try{const draft=JSON.parse(sessionStorage.getItem(key)||'{}')as Draft;if(draft.when)setWhen(draft.when);if(draft.scheduleAt)setScheduleAt(draft.scheduleAt)}catch{}},[key]);const choose=(value:When)=>{setWhen(value);setMessage('');if(value==='programar'&&!scheduleAt)setScheduleAt(localValue(new Date(Date.now()+7200000)))};const next=()=>{if(when==='programar'&&!scheduleAt){setMessage('Elegí la fecha y hora del servicio.');return}if(when==='programar'&&new Date(scheduleAt).getTime()<=Date.now()){setMessage('Elegí una fecha y hora futura.');return}try{const previous=JSON.parse(sessionStorage.getItem(key)||'{}')as Draft;let finalSchedule='';if(when==='programar')finalSchedule=new Date(scheduleAt).toISOString();else if(when==='manana'){const d=new Date();d.setDate(d.getDate()+1);d.setHours(9,0,0,0);finalSchedule=d.toISOString()}sessionStorage.setItem(key,JSON.stringify({...previous,when,whenConfirmed:true,scheduleAt:finalSchedule,urgent:when==='ahora'}))}catch{}setPayment(true)};if(!session)return null;if(payment)return <ClientPaymentScreen onBack={()=>setPayment(false)} onConfirm={onContinue}/>;const options:[When,string,string,string][]=[['ahora','⚡','Lo antes posible','Hoy, en las próximas horas'],['hoy','▣','Hoy','En el día de hoy'],['manana','▣','Mañana','Todo el día'],['programar','▣','Elegir fecha y hora','Seleccionar en calendario']];return <main className="ugo-when-screen" aria-label="Cuándo lo necesitás"><header><button type="button" onClick={onBack}>←</button><strong>UGO</strong><div className="ugo-when-progress"><i/><i/><i/><i/><i/></div><small>3 de 5</small></header><section><h1>¿Cuándo lo necesitás?</h1><p>Elegí la fecha y hora</p><div className="ugo-when-options">{options.map(([value,icon,title,subtitle])=><button type="button" key={value} className={when===value?'selected':''} onClick={()=>choose(value)}><span className="ugo-when-icon">{icon}</span><span><b>{title}</b><small>{subtitle}</small></span><i/></button>)}</div>{when==='programar'&&<label className="ugo-when-date">Fecha y hora<input type="datetime-local" value={scheduleAt} min={localValue(new Date())} onChange={e=>setScheduleAt(e.target.value)}/></label>}{message&&<p className="ugo-when-error">{message}</p>}</section><footer><button type="button" onClick={next}>Continuar →</button></footer></main>}
export default ClientWhenScreen
