import React from'react'
import{useProviderFlow}from'./providerFlow'

const demo=[{id:'demo-electricidad',category:'Electricidad',zone:'Canasvieiras',distance:'2,3 km',age:'Hace 8 min',value:'R$ 180 estimados'},{id:'demo-reparacion',category:'Reparación',zone:'Cachoeira',distance:'4,1 km',age:'Hace 14 min',value:'R$ 140 estimados'}]
export function ProviderOpportunities(){const flow=useProviderFlow();return <section className="provider-screen" aria-labelledby="provider-opportunities-title"><header className="provider-section-head"><div><span className="provider-kicker">MATCH UGO</span><h1 id="provider-opportunities-title">Oportunidades</h1><p>Trabajos concretos compatibles con tu perfil.</p></div></header><div className="provider-list">{demo.map(item=><article className="provider-card provider-opportunity" key={item.id}><div><span className="provider-chip">{item.age}</span><h2>{item.category}</h2><p>{item.zone} · {item.distance}</p><strong>{item.value}</strong></div><button className="provider-primary" onClick={()=>flow.actions.openOpportunity(item.id)}>Ver oportunidad</button></article>)}</div></section>}

export function ProviderOpportunityDetail({id}:{id:string|null}){const flow=useProviderFlow();return <section className="provider-screen"><button className="provider-back" onClick={flow.actions.openOpportunities}>← Oportunidades</button><span className="provider-kicker">DETALLE</span><h1>Oportunidad</h1><article className="provider-card provider-detail"><h2>Servicio compatible</h2><p>Revisá alcance, zona, distancia, importe y condiciones antes de aceptar.</p><small>ID: {id||'sin seleccionar'}</small></article><div className="provider-actions"><button className="provider-secondary" onClick={()=>id&&flow.actions.rejectOpportunity(id)}>Rechazar</button><button className="provider-primary" onClick={()=>id&&flow.actions.acceptOpportunity(id)}>Aceptar oportunidad</button></div></section>}
