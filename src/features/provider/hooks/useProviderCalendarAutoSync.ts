import{useEffect}from'react'
import{syncProviderCalendar}from'../../../mvp/provider/providerCalendar'
export function useProviderCalendarAutoSync(accessToken:string,serviceId?:string|null,serviceState?:string|null){useEffect(()=>{if(!accessToken)return;let alive=true,running=false;const run=async()=>{if(!alive||running||!navigator.onLine)return;running=true;try{await syncProviderCalendar(accessToken)}catch{}finally{running=false}},onOnline=()=>void run(),onVisible=()=>{if(document.visibilityState==='visible')void run()};void run();const timer=window.setInterval(()=>void run(),5*60*1000);window.addEventListener('online',onOnline);document.addEventListener('visibilitychange',onVisible);return()=>{alive=false;window.clearInterval(timer);window.removeEventListener('online',onOnline);document.removeEventListener('visibilitychange',onVisible)}},[accessToken,serviceId,serviceState])}
